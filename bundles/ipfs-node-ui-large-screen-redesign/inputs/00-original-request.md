# Original Request

Source: user request in Codex thread, 2026-05-19.

```text
Use [$candoitall-bundle-workflow](C:\Users\dell\.codex\skills\candoitall-bundle-workflow\SKILL.md) to improve UI of the IPFS node.
Use [$imagegen](C:\Users\dell\.codex\skills\imagegen\SKILL.md) to create new designs of the layouts. Each page separatelly and each tab on page separatelly and each dialog separatelly. We can use more dialogs to hide some things that are not necessary to have on main screen all the time. 
we do not need to optimize it for medium and small screen. it will be for large screen only. Use it as hard rule to do not waste time on it.
All large stats cards change to some small badges, so it does not take too much space. Assure we are using all space effective way and there are no empty spaces. 
You must not just stack the components vertically. Analyze proposed layouts. Use space effective way and try to have main items on page so user do not need to scroll too much. 
you must validate all with playwright mcp and screenshots and compare those screenshots with design proposals. If they do not look similar you must improve it. 
Avoid to using own pure css, div/button/span, etc use CanDoItAll BaseLib components primarilly and small tuning of styles you can do with tailwind. But it is better to keep default styles rather than using any own css.
```

## Raw Note IDs

| Note ID | Exact wording |
| --- | --- |
| N1 | Use `candoitall-bundle-workflow` to improve UI of the IPFS node. |
| N2 | Use `imagegen` to create new designs of the layouts. Each page separately and each tab on page separately and each dialog separately. |
| N3 | Use more dialogs to hide things that are not necessary to have on main screen all the time. |
| N4 | Do not optimize for medium and small screen; large screen only is a hard rule. |
| N5 | Change all large stats cards to small badges. |
| N6 | Use space effectively and avoid empty spaces. |
| N7 | Do not just stack components vertically; analyze layouts and keep main items on the page so the user does not need to scroll too much. |
| N8 | Validate all with Playwright MCP and screenshots, compare screenshots with design proposals, and improve when they diverge. |
| N9 | Avoid own pure CSS, `div`/`button`/`span`; primarily use CanDoItAll BaseLib components and small Tailwind tuning; prefer default styles over custom CSS. |
